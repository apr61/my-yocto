# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-my-oss"
BBFILE_PATTERN_meta-my-oss = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-my-oss = "6"

LAYERDEPENDS_meta-my-oss = "core"
LAYERSERIES_COMPAT_meta-my-oss = "dunfell"
